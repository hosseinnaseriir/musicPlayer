import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1DB954",
    },
    common: {
      black: "#212121",
      white: "#F2F2F2",
    },
  },
  typography: {
    htmlFontSize: 16,
  },
});
